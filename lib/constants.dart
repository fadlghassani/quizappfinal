import 'package:flutter/material.dart';

const Color correct = Color(0xFF388E3C);    // Dark green
const Color incorrect = Color(0xFFD32F2F);  // Dark red
const Color neutral = Color(0xFF424242);    // Dark grey, lighter than background
const Color background = Color(0xFF121212); // Dark background
const Color accent = Color(0xFF1E88E5);     // Blue accent
const Color textColor = Colors.white;       // White text everywhere

